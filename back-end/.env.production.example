# Production Environment Configuration
# Copy to .env in back-end directory and fill in values

# JWT Secret (REQUIRED - generate with: openssl rand -hex 32)
JWT_SECRET=CHANGE_ME_GENERATE_A_SECURE_SECRET

# Database Configuration
DB_HOST=127.0.0.1
DB_USERNAME=rtrs_user
DB_PASSWORD=secure-password
DB_NAME=rtrs_production
DB_DIALECT=mysql
DB_PORT=3306

# Server Configuration
PORT=8000
NODE_ENV=production

# CORS Origins (comma-separated)
# WARNING: Leave empty ONLY for local development. In production, set your actual domain(s).
# Example: CORS_ORIGINS=https://restaurant.example.com,https://admin.example.com
CORS_ORIGINS=

# Redis Configuration (for caching) - Leave empty to disable
REDIS_HOST=localhost
REDIS_PORT=6379

# Sentry Error Tracking (optional)
SENTRY_DSN=

# Log Level
LOG_LEVEL=error